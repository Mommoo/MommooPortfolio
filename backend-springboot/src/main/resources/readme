해당 디렉토리 위치에 application.properties 파일이 반드시 존재해야 합니다.
application.properties 파일에는 개인정보를 다루는 이유로,
공개 저장소인 GitHub에는 부득이하게 올리지 못했습니다.

비공개된 application.properties 파일의 구성은 아래와 같습니다.

## MongoDB Config

spring.data.mongodb.uri=mongodb://{ip}:{port}
spring.data.mongodb.database={databaseName}